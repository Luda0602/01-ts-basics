const name: string = "Alice";       // тип string (рядок)
const age: number = 30;             // тип number (число)
const isOnline: boolean = true;     // тип boolean (логічне значення)

console.log(`Name: ${name}`);
console.log(`Age: ${age}`);
console.log(`Online: ${isOnline}`);
